DEFAULT thinclient
PROMPT 0
TIMEOUT 10

LABEL thinclient
    KERNEL /bzImage
    INITRD /initrd.gz
    APPEND console=tty1 vt.default_utf8=1 quiet
    # Если консоль не появилась (редкий BIOS без KMS-драйвера под чип) —
    # добавь в APPEND: vga=791  (VESA-framebuffer 1024x768)
